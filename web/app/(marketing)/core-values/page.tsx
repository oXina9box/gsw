import Link from "next/link";
import { notFound } from "next/navigation";
export const metadata = { title: "Core Values" };
const values = ["[Value one]", "[Value two]", "[Value three]", "[Value four]"];
export default function CoreValuesPage() { if (process.env.SITE_CONTENT_APPROVED !== "true") notFound(); return <article className="reading-page shell" data-archetype="A2"><p className="kicker">Core values / working template</p><h1>What we believe.</h1><div className="notice">Draft template: the final Gem Studio values will be supplied and approved by the owner before launch.</div><p className="lede">This page is a framework for the principles that should shape the product, the work, and the way the studio treats people.</p><div className="grid">{values.map((value) => <section className="card" key={value}><p className="kicker">Draft value</p><h2>{value}</h2><p>[Short principle statement]</p><p>[What this means in practice, including a behavior or example.]</p></section>)}</div><h2>How values show up in the studio</h2><p>[Describe how these values change briefs, handoffs, AI usage, review, audience feedback, and release decisions.]</p><div className="actions"><Link className="button button-primary" href="/signup">Create your account ↗</Link><Link className="button button-outline" href="/">Back to the studio</Link></div></article>; }
